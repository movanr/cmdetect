name: cmdetect

# Development Docker Compose Configuration
#
# Environment Variables:
# - Loaded from /opt/cmdetect/.env (portable config)
# - Overridden by /var/www/cmdetect/.env (server-specific secrets) if exists
# - For local development, typically just uses .env in project root
#

services:
  postgres:
    image: postgres:15-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-cmdetect}_postgres
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-cmdetect}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    networks:
      - cmdetect_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-cmdetect}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  hasura:
    image: hasura/graphql-engine:v2.46.0
    container_name: ${COMPOSE_PROJECT_NAME:-cmdetect}_hasura
    restart: unless-stopped
    ports:
      - "${HASURA_PORT:-8080}:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      HASURA_GRAPHQL_METADATA_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-cmdetect}
      HASURA_GRAPHQL_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-cmdetect}
      HASURA_GRAPHQL_ADMIN_SECRET: ${HASURA_GRAPHQL_ADMIN_SECRET:?HASURA_GRAPHQL_ADMIN_SECRET required}
      HASURA_GRAPHQL_JWT_SECRET: ${HASURA_GRAPHQL_JWT_SECRET:?HASURA_GRAPHQL_JWT_SECRET required}
      HASURA_GRAPHQL_UNAUTHORIZED_ROLE: ${HASURA_GRAPHQL_UNAUTHORIZED_ROLE:-public}
      HASURA_GRAPHQL_ENABLE_CONSOLE: ${HASURA_GRAPHQL_ENABLE_CONSOLE:-true}
      HASURA_GRAPHQL_DEV_MODE: ${HASURA_GRAPHQL_DEV_MODE:-true}
      HASURA_GRAPHQL_ENABLED_LOG_TYPES: ${HASURA_GRAPHQL_ENABLED_LOG_TYPES:-startup,http-log,webhook-log,websocket-log,query-log}
      HASURA_GRAPHQL_CORS_DOMAIN: ${HASURA_GRAPHQL_CORS_DOMAIN:-*}
      HASURA_GRAPHQL_METADATA_DEFAULTS: '{"backend_configs":{"dataconnector":{"athena":{"uri":"http://data-connector-agent:8081/api/v1/athena"},"mariadb":{"uri":"http://data-connector-agent:8081/api/v1/mariadb"},"mysql8":{"uri":"http://data-connector-agent:8081/api/v1/mysql"},"oracle":{"uri":"http://data-connector-agent:8081/api/v1/oracle"},"snowflake":{"uri":"http://data-connector-agent:8081/api/v1/snowflake"}}}}'
    depends_on:
      postgres:
        condition: service_healthy
      data-connector-agent:
        condition: service_healthy
    networks:
      - cmdetect_network
    healthcheck:
      test: ["CMD-SHELL", "timeout 1s bash -c ':> /dev/tcp/127.0.0.1/8080' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  data-connector-agent:
    image: hasura/graphql-data-connector:v2.46.0
    container_name: ${COMPOSE_PROJECT_NAME:-cmdetect}_data_connector
    restart: unless-stopped
    ports:
      - "${DATA_CONNECTOR_PORT:-8081}:8081"
    environment:
      QUARKUS_LOG_LEVEL: ${DATA_CONNECTOR_LOG_LEVEL:-ERROR}
      QUARKUS_OPENTELEMETRY_ENABLED: "false"
    networks:
      - cmdetect_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/api/v1/athena/health"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 10s

  auth-server:
    build:
      context: .
      dockerfile: apps/auth-server/Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-cmdetect}_auth_server
    restart: unless-stopped
    ports:
      - "${AUTH_SERVER_PORT:-3001}:3001"
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: 3001
      POSTGRES_DB: ${POSTGRES_DB:-cmdetect}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET required}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      PATIENT_FRONTEND_URL: ${PATIENT_FRONTEND_URL:-http://localhost:3002}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cmdetect_network
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3001/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1); }).on('error', () => process.exit(1))\""]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 15s

volumes:
  postgres_data:
    driver: local
    name: ${COMPOSE_PROJECT_NAME:-cmdetect}_postgres_data

networks:
  cmdetect_network:
    name: ${COMPOSE_PROJECT_NAME:-cmdetect}_network
    driver: bridge
