export type Unit = "mm";

/*export const UNITS = {
  mm: {
    id: "mm",
    label: {
      en: "mm",
      de: "mm",
    },
  },
} as const;

export type UnitId = keyof typeof UNITS;

export type Unit = (typeof UNITS)[UnitId];
*/
